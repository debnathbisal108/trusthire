# ── Framework ──────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# ── Database ───────────────────────────────────────────────────
sqlalchemy==2.0.35
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# ── Queue ──────────────────────────────────────────────────────
celery==5.4.0
redis==5.2.0
celery-redbeat==2.2.0

# ── AI / LLM (cloud APIs — no local model needed) ─────────────
langchain==0.3.7
langchain-community==0.3.5
langchain-openai==0.2.9

langchain-groq==0.2.1
langchain-google-genai==2.0.7
langchain-cohere==0.3.1
groq==0.11.0

# ── Embeddings (lightweight, no torch needed) ──────────────────
qdrant-client==1.12.0

# ── Document parsing (no PaddleOCR — too heavy for Render) ────
PyMuPDF==1.24.13
python-docx==1.1.2
pillow==11.0.0
# pytesseract is optional — install if needed via render build command

# ── Email ──────────────────────────────────────────────────────
resend>=2.5.1

# ── Security ───────────────────────────────────────────────────
cryptography==43.0.3
PyJWT==2.9.0
python-magic==0.4.27
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# ── Storage (S3-compatible — works with Cloudflare R2) ─────────
boto3==1.35.0

# ── Reporting ──────────────────────────────────────────────────
weasyprint==63.1
jinja2==3.1.4

# ── HTTP ───────────────────────────────────────────────────────
httpx==0.27.2
aiohttp==3.11.0
aiofiles==24.1.0

# ── Monitoring ─────────────────────────────────────────────────
prometheus-client==0.21.0

# ── Utilities ──────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.6.0
python-dateutil==2.9.0.post0
pytz==2024.2
structlog==24.4.0

# ── Testing ────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
factory-boy==3.3.1
